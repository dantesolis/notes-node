// [TODO]: Maybe change to ES7/8 to export/import declarations?

console.log('Startng notes.js');

const addNotes = () => {
  console.log(`Adding note with title and body: ${title, body}`);
};

module.exports = {
  addNote
};